
;(defpoll batlvl
;    :interval "10s"
;    "acpi | rg --only-matching '\d+%'"
;)

(defwidget battery []
  (box
    :class "status"
    :orientation "h"
    :space-evenly false
    :halign "end"
    (label :class "icon" :text "" :yalign 0.5)
    (label :text {EWW_BATTERY.BAT0.capacity})
    "%"
  )
)


