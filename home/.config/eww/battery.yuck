(defpoll laptop_batlvl :interval "10s" :initial 100 "bash ~/.config/eww/scripts/laptop-battery.sh")

(defwidget battery []
    (box
      :visible {laptop_batlvl != 404}
      :class "status"
      :orientation "h"
      :space-evenly false
      :halign "end"
      (---)
      (label :class "icon" :text "")
      (label :class "status" :text laptop_batlvl)
    )
)

