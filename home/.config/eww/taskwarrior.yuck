(defpoll task :interval "10s" "task rc.verbose: rc.report.next.columns:description rc.report.next.labels:1 limit:1 next")

(defwidget taskwarrior []
  (revealer :transition "slideleft" :reveal {zen != true}
    (box
    :space-evenly false
      (---)
      (label :class "topbar icon" :text "")
      (label :class "status" :text task)
    )
  )
)
