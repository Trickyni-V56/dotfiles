(defpoll sunset :interval "10s" "~/.shells/suntime.sh || echo night")

(defwidget sunclock []
  (box
    :visible {sunset != "night"}
    :class "status"
    :orientation "h"
    (---)
    (label
      :text "󰖚"
      :class "icon"
    )
    (label
      :text sunset
      :class "status"
    )
  )
)
