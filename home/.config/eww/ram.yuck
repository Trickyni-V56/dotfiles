(defpoll _ram :interval "1s" "bash scripts/ram.sh")

(defwidget RAM []
  (box
    :class "status"
    :orientation "h"
    :space-evenly false
    :tooltip "ram"
    :halign "end"
    (---)
    (label :class "icon" :text "󰍛")
    (label
        :text "${_ram}%"
    )
  )
)
