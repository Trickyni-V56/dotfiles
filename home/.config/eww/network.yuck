(defpoll _network :interval "500ms" :initial false "curl http://gstatic.com/generate_204 && echo true || echo false")

(defwidget network []
   (revealer :reveal _network :transition "slideup"
     (box  :class "bottomstatus" :space-evenly false
       (label :class "bottombar-icon" :text "")
    )
  )
)

