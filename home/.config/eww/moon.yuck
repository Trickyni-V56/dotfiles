(defpoll moonphase :interval "1s" "scripts/moon.sh")
(defpoll moontext :interval "10s" "curl -s wttr.in/Hod_Hasharon?format=%m")

(defwidget moon []
  (box
    :class "moon"
    :orientation "h"
    :space-evenly false
    :halign "end"
    (label :text moonphase)
    (label :text moontext)
  )
)
