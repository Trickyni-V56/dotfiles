daemon is a wrapper script for void linux's service enabling scheme.

subcommands
    
    daemon bind <NAME OF SERVICE>
        enables a service 
        
    daemon list active
        lists all active services
        
    daemon list available
        lists all available services
